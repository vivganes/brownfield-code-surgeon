{
  "name": "brownfield-code-surgeon",
  "version": "0.1.0",
  "private": true,
  "description": "A package of tools that productionize the seven-agent brownfield code-surgery workflow from Ganesan et al., Zenodo 2026.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @brownfield-surgeon/shared && npm run build -w @brownfield-surgeon/core-prompts && npm run build -w @brownfield-surgeon/plugin",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
